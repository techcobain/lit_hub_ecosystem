name: Build ecosystem index

on:
  push:
    branches:
      - main
    paths:
      - "ecosystem/**"
      - "scripts/build-ecosystem.mjs"
      - ".github/workflows/build-ecosystem.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node scripts/build-ecosystem.mjs
      - name: Commit generated index
        run: |
          if git diff --quiet -- public/ecosystem.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/ecosystem.json
          git commit -m "Build ecosystem index"
          git push
